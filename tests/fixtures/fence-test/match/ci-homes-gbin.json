{
  "schema_version": 1,
  "kind": "login-hook-elev",
  "purpose": "Allow dns-adm login hook to run dns-cli interactive via sudo -n.",
  "username": "dns-adm",
  "service": "dns-cli",
  "action": "add",
  "submit_app": "dns-cli",
  "submit_version": "1.12.0",
  "commands": [
    {
      "runas": "root",
      "tags": ["NOPASSWD"],
      "path": "/home/alice/prjs/dns-cli/.ci-homes/home.TEST/gbin/dns-cli",
      "args": ["interactive"]
    }
  ]
}
